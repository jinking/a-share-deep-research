{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jinking/a-share-deep-research/schemas/evidence_link.schema.json",
  "title": "EvidenceLink",
  "description": "Claim → Document 的带定位证据链接（evidence/evidence_links.jsonl 的一行）",
  "type": "object",
  "additionalProperties": false,
  "required": ["evidence_id", "claim_id", "document_id", "support_type"],
  "properties": {
    "evidence_id": {
      "type": "string",
      "pattern": "^EV_[A-Za-z0-9_]+$",
      "description": "建议格式 EV_<claim_id>_<序号>"
    },
    "claim_id": { "type": "string", "minLength": 1 },
    "document_id": { "type": "string", "pattern": "^DOC_[A-Za-z0-9_]+$" },
    "page": { "type": ["integer", "null"], "minimum": 1 },
    "section": { "type": ["string", "null"] },
    "paragraph": { "type": ["string", "null"] },
    "table": { "type": ["string", "null"] },
    "evidence_text": {
      "type": ["string", "null"],
      "description": "原文摘录；critical Claim 必填，供人工复核"
    },
    "support_type": {
      "type": "string",
      "enum": ["direct", "partial", "contradict", "context"]
    },
    "confidence": { "type": ["number", "null"], "minimum": 0, "maximum": 1 },
    "excerpt_verification_status": {
      "type": ["string", "null"],
      "enum": ["verified", "unverified", "not_applicable", null],
      "description": "摘录验证状态；critical Claim 的摘录必须为 verified（v3.0.2 §9）。缺省/null 一律按未验证处理"
    },
    "excerpt_verification_method": {
      "type": ["string", "null"],
      "enum": ["direct_text", "text_layer", "manual", "ocr", null],
      "description": "验证手段；status=verified 时必须说明是怎么验的"
    },
    "excerpt_verification_source": {
      "type": ["string", "null"],
      "description": "用来比对的文本来源（如 raw/1225497942.textlayer.txt）"
    },
    "migration_status": { "type": ["string", "null"] },
    "note": { "type": ["string", "null"] }
  }
}
