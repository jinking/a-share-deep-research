[pytest]
testpaths = tests
pythonpath = .
# --basetemp 必须保留：沙箱禁止写系统临时目录，否则 tmp_path 报 PermissionError。
# 不要在这里写 -q：与命令行 / CI 的 `pytest -q` 叠加会变成 -qq，
# 连带把最后的「N passed」汇总行也压掉，CI 日志就看不到用例数了。
addopts = --basetemp=.pytest-tmp
